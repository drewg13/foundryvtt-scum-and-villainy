export const log = (message) => console.log(`Foundry VTT | Clocks | ${message}`);
export const warn = (message) => console.warn(`Foundry VTT | Clocks | ${message}`);
export const error = (message) => console.error(`Foundry VTT | Clocks | ${message}`);
